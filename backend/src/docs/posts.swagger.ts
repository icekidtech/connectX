/**
 * Posts Module Swagger Documentation
 * 
 * This file contains all Swagger decorators and schemas for post endpoints.
 * Endpoints will be added as Phase 3 is implemented.
 * 
 * Planned Endpoints (Phase 3):
 * - GET /posts/feed
 * - POST /posts
 * - GET /posts/:id
 * - DELETE /posts/:id
 * - PATCH /posts/:id
 * - POST /posts/:id/like
 * - GET /posts/:id/likes
 * - POST /posts/:id/comments
 * - GET /posts/:id/comments
 * - DELETE /comments/:commentId
 * - PUT /comments/:commentId
 * - POST /posts/:id/media
 * - DELETE /posts/:id/media/:mediaId
 */

export const postsSwaggerFile = 'src/docs/posts.swagger.ts';
