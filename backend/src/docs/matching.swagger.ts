/**
 * Matching Module Swagger Documentation
 * 
 * This file contains all Swagger decorators and schemas for matching endpoints.
 * Endpoints will be added as Phase 5 is implemented.
 * 
 * Planned Endpoints (Phase 5):
 * - GET /matching/recommendations
 * - GET /matching/recommendations/:userId
 * - POST /matching/:userId/like
 * - POST /matching/:userId/pass
 * - GET /matching/my-matches
 * - DELETE /matching/:matchId
 */

export const matchingSwaggerFile = 'src/docs/matching.swagger.ts';
